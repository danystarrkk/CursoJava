// Objeto Math

let resultado;


resultado = Math.PI; // Valor de pi
resultado = Math.round(2.2); // Redondea el valor a un numero entero
resultado = Math.ceil(2.1); // Redondea siempre hacia ariva
resultado = Math.floor(2.9); // Redondea siempre hacia abajo
resultado = Math.sqrt(144); // Raiz cuadrada de un numero
resultado = Math.abs(-200); // Converte los calores a positivos
resultado = Math.min(3, 5, 8, 9, 11); // Retorna el numero menor de la lista
resultado = Math.max(1, 3, 4, 5, 3, 7, 7); // Retorna el numero mayor de la lista
resultado = Math.random(); // Valor al azar no mayor de 0
resultado = Math.floor(Math.random() * 30); // Definimos el rango de un valor aleatorio entre 1 y 30



console.log(resultado);
